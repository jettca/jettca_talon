app: terminal
-
tag(): user.line_commands
do vim:
    insert("vim ")

cd:
    insert("cd ")

mv:
    insert("mv ")

ls:
    insert("ls ")

rm:
    insert("rm ")

make dir:
    insert("mkdir ")

git:
    insert("git ")

do sea make:
    insert("cmake ")
